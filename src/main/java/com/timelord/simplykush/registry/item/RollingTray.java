package com.timelord.simplykush.registry.item;

import net.minecraft.block.Block;

public class RollingTray extends Block {
    public RollingTray(Settings settings) {
        super(settings);
    }
}
